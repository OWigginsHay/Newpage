The base surface for grouped content. White, softly rounded (16px), subtle border + shadow.

```jsx
<Card elevation="md" hover>…</Card>
```

`elevation`: none|sm|md|lg. `hover` lifts and deepens the shadow. `padded={false}` to control padding yourself (e.g. media at the top edge).
