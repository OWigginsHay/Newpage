# --- web / api ---
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
python-dotenv>=1.0

# --- llm ---
openai>=1.40

# --- rag: embeddings + vector store ---
sentence-transformers>=3.0
chromadb>=0.5

# --- document ingestion ---
pymupdf>=1.24
rapidocr-onnxruntime>=1.3   # optional OCR for scanned pages (CPU, pip-only)

# --- dev / test ---
pytest>=8.0
