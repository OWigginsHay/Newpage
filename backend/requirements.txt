# --- web / api ---
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
python-dotenv>=1.0

# --- llm ---
openai>=1.40

# --- rag: embeddings + vector store ---
sentence-transformers>=3.0
chromadb>=0.5

# --- document ingestion ---
pymupdf>=1.24
# OCR for scanned pages / image files (first available is used; see app/ocr.py):
easyocr>=1.7                 # GPU-accelerated when Torch/CUDA present (best quality)
# rapidocr-onnxruntime>=1.3  # CPU-only alternative (pip-only, no model download)

# --- dev / test ---
pytest>=8.0
