# Pinned to the exact versions verified in a clean Python 3.11–3.13 venv
# (python -m venv .venv && pip install -r requirements.txt). Pinning direct
# dependencies keeps installs reproducible; transitive deps resolve from these.
#
# GPU note: `torch` is pulled in transitively (by sentence-transformers / easyocr)
# and installs the CPU build by default — which works everywhere. For GPU, install
# the CUDA torch wheel first (see pytorch.org), then run this file.

# --- web / api ---
fastapi==0.139.2
uvicorn[standard]==0.51.0
pydantic==2.13.4
python-dotenv==1.2.2

# --- llm ---
openai==2.46.0

# --- rag: embeddings + vector store ---
sentence-transformers==5.6.0
chromadb==1.5.9

# --- document ingestion ---
pymupdf==1.28.0
beautifulsoup4==4.15.0        # HTML → text (prose, tables, SVG chart labels)
easyocr==1.7.2                # OCR for images / scanned pages (GPU-accelerated if CUDA torch is present)
# rapidocr-onnxruntime         # CPU-only OCR alternative (no model download)

# --- dev / test ---
pytest==9.1.1
